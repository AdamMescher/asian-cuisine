import styled from 'styled-components';

const StyledCombinationMark = styled.div`
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: ${props => props.height};
    width: auto;
`;

export default StyledCombinationMark;